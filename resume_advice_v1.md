# 简历适配建议：证据约束版 v1

完成日期：2026-09-25

## 1. 解决什么问题

同一段真实经历可以针对不同岗位强调不同的工作侧面，但简历不能出现用户没做过的行动、工具、结果或身份。第一版先给出可追溯的表达草稿和“值得强调的方向”，让用户逐句核对。

## 2. 为什么现在做

个人画像、岗位画像与匹配结果已经可以保存。匹配输出含有 `resume_focus_candidates` 和 `prohibited_resume_additions`，可以作为岗位侧的建议；个人画像中的经历行动、交付物和证据则是事实来源。

## 3. 需要理解的概念

- **事实来源**：一句简历草稿只组合个人画像中已列出的经历标题、行动与交付物；日期、数字、工具和结果若未出现就不补。
- **强调方向**：匹配分析告诉用户该岗位更看重什么，但它只能指导选择和排序，不能自动变成“我做过”。
- **证据缺口**：缺少行动或交付物时，系统给补充问题，不生成可直接使用的句子。
- **确认状态**：整个画像未确认时不生成句子；部分确认的画像只使用不再要求确认的经历。画像整体确认时可生成草稿，但仍提示个别经历的日期、结果或证明材料可能缺失。

## 4. 用户要做什么

用户需要逐句核对草稿，并决定是否放入简历。系统不会自动替换简历，也不会把草稿发给招聘方。用户已确认下一版可提供更自然的措辞，但仍要保留逐句证据与最终人工确认。

## 5. 已实现的工程部分

- `src/matching/resume_advice.py`：本地生成候选句、证据 ID、岗位强调方向和禁止添加的表述；
- `GET /matches/{match_id}/resume-advice`：读取已保存的匹配与画像，不调用模型服务商；
- 匹配结果页展示候选句、证据和需要补充事实的经历；
- `tests/test_resume_advice.py`：验证未确认经历不会形成简历句，确认后不添加不存在的“AI 产品经理经验”。

自然改写的第二层：

- `POST /matches/{match_id}/resume-rewrite` 只在用户额外同意后，把可用经历的标题、行动、交付物及岗位关注点发送给选定模型；不发送整份简历或完整个人画像；
- 模型返回的句子必须列明逐字来源事实 ID；本地检查额外数字、未出现的常见工具、强动词和明令禁止的经历表述；
- 检查失败时只返回原先保守句，并说明拒绝原因。通过检查的 AI 句子也仍标为“待核对”，不自动保存或替换简历；
- `tests/test_resume_rewriter.py` 验证自然改写、虚构“主导并提升 30%”时回退，以及联系方式发送前脱敏。

## 6. 如何验证

后端运行：

```bash
.venv/bin/python -m unittest tests.test_resume_advice tests.test_resume_rewriter
```

前端在 `frontend/` 目录运行 `npm run build` 和 `npm run lint`。

机械规则不能自动证明所有中文改写都真实，尤其是含糊的职责动词。因此模型结果始终是待核对草稿。下一步应由用户拿一条目标 JD 和真实简历逐句审阅：每个动词、工具和成果是否真能举证。真实 DeepSeek 调用尚待本机重新设置 Key 后验证。
