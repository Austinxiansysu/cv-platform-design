# 第三步：从输入到匹配解释的本机原型

完成日期：2026-09-24

## 这一步解决什么问题

此前只能在 API 文档页面手动提交 JSON。现在用户可以在浏览器中填写基本信息、粘贴简历经历与偏好、粘贴 JD，查看两份结构化草稿并确认保存，随后看到职业方向和当前申请资格两个结论。求职记录也可收藏并更新状态。

## 为什么现在做

岗位 Schema、个人画像 Schema、匹配规则和 10 条 JD 回归集已经建立。页面把这些模块连成一个可走通的流程，为后续真实用户测试准备材料。

## 三个关键概念

1. **草稿**：模型生成、尚未保存的结果。用户应检查经历、任务、门槛和证据。
2. **确认**：个人画像确认后才能用于匹配；岗位和匹配结果也由用户决定是否保存。
3. **双结论**：职业方向摘要回答“是否值得探索”；当前申请资格回答“这轮能不能投”。二者独立。

## 当前用户需要做什么

无需做编程练习。若想验证真实 AI 输出，需要在本机准备 DeepSeek API Key；请勿把密钥发到聊天或提交到仓库。正式试用前，建议先用不含联系方式的示例简历和一条公开 JD 检查字段质量。

## 已完成的工程部分

- `POST /profiles/analyze`：简历经历和偏好生成个人画像草稿；
- `POST /jobs/analyze`：JD 生成岗位画像草稿；
- `POST /matches/analyze`：已确认画像与岗位生成证据对齐草稿，本地规则计算分数；
- React 单页界面：展示证据、缺口、未知项和求职记录；
- 联系方式与本地文件路径在发送给模型前脱敏，且每次发送画像都要求用户勾选同意。

## 如何验证

后端测试：

```bash
.venv/bin/python -m unittest tests.test_profile_builder tests.test_match_parser tests.test_job_parser tests.test_backend_api tests.test_scoring
```

前端在 `frontend/` 目录运行 `npm run build` 和 `npm run lint`。启动方法见 [frontend/README.md](frontend/README.md)。

## 尚未验证与下一阶段

本机没有配置 DeepSeek API Key，因此本轮只验证了离线替身调用、接口、数据关联、页面构建和缺密钥提示。没有证明 DeepSeek 对真实简历和 JD 的解析质量；拿到 Key 后先用现有 10 条 JD 和至少 1 份去标识化简历做实测与错误分析。

当前前端仍是研究原型：支持粘贴文字，不支持上传 Word/PDF，也不自动打开岗位链接。画像修正需要修改输入并重新生成；未来可加入逐字段编辑和简历版本管理。
