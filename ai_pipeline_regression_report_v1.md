# AI 三模块回归测试报告 v1

测试日期：2026-09-21

## 一 已测试模块

| 模块 | 测试对象 | 最终结果 |
|---|---|---|
| 岗位画像提取 | 飞书 FDE | 通过 |
| 岗位画像提取 | 腾讯商业分析 | 通过 |
| 岗位画像提取 | 投后管理 | 通过 |
| 个人画像提取 | 当前用户材料 | 通过 |
| 证据对齐 | 当前画像 × 飞书 FDE | 通过 |
| 透明规则计分 | 飞书人工夹具、腾讯人工夹具、飞书真实输出 | 通过 |

“通过”表示：符合严格 Schema，并通过当前自动语义规则；不代表已经达到大规模生产质量。

## 二 最终测试文件

### 岗位画像

- `eval_outputs/run-004/fde_normalized.json`；
- `eval_outputs/run-004/tencent_actual.json`；
- `eval_outputs/run-003/post_investment_normalized.json`。

### 个人画像

- `eval_outputs/profile-run-002/profile_normalized.json`。

### 证据对齐

- `eval_outputs/match-run-003/fde_match_actual.json`。

### 透明计分

- `src/matching/scoring.py`；
- `tests/test_scoring.py`；
- `eval_outputs/scoring/fde_expected_score.json`；
- `eval_outputs/scoring/tencent_expected_score.json`；
- `eval_outputs/scoring/fde_actual_score.json`。

### 自动校验

上述五份最终输出的 P0、P1、P2 计数均为 0。

## 三 岗位画像测试结论

### 飞书 FDE

系统正确识别：

- 官方职类为销售支持；
- 主职能是 AI 解决方案与低代码方案搭建；
- 客户调研、Demo、SOP 和培训是不同任务；
- 2027 届是当前招聘批次条件；
- 到岗天数、实习时长、出差和强度未知；
- FDE 标题与任务一致，但对普通学生透明度有限。

### 腾讯商业分析

系统正确识别：

- 远程面试不是工作地点；
- 工作地点、届别、学历、到岗和时长未知；
- 行业研究、用户研究、战略建议和跨团队推进分开；
- 职责推导能力标为 inferred 与 uncertain；
- 学业成绩优异属于定性申请要求，不是机器可直接判断的硬门槛。

### 投后管理

系统正确识别：

- 来源为非官网转发，招聘主体和状态需核验；
- 6 个月与每周 4 天是明确条件；
- 原始职责拆为 6 项，并归并为 4 个任务簇；
- 投行、事务所和股权投资机构实习经历是 preferred；
- 投后管理不仅包含经营分析，还包含募资、合规、材料和 PR、GR 支持。

## 四 个人画像测试结论

最终输出做到：

- 没有编造项目日期、工具、量化结果和成果；
- 没有把比赛改写成企业经验；
- 没有把官网项目改写成 AI 产品经理或 FDE 经验；
- Python 等仅为自述证据；
- 数学建模和案例分析只作为 transferable 证据；
- 未体验任务保持 untested 与低偏好置信度；
- confirmed_skill_gaps 保持为空；
- 明确提出补充项目行动、结果和验证材料的问题。

## 五 证据对齐测试结论

飞书 FDE 对齐结果正确实现：

- 2027 届冲突只影响当前申请资格；
- 职业方向探索继续进行；
- AI 方案搭建为正向匹配；
- 客户调研、培训和销售支持为未体验；
- SOP 偏好保持未体验，没有从档案合规厌恶过度推断；
- 未证明能力归入 evidence_gap，不写成 capability_gap；
- 未知时间、出差和强度进入 information_gaps；
- 简历禁写项明确阻止虚构飞书、FDE和客户调研经验。
- 职业方向一致度为 high，成长价值为 high，置信度为 medium；
- 因任务偏好覆盖率不足 50%，后续规则层不得强行生成想做程度和总体方向数值。

## 六 测试推动的产品改进

真实测试共发现并处理以下类型：

- 测试输入缺少来源元数据；
- 面试方式误归类为工作地点；
- 职责推导能力误标 must；
- 标题一致性和标题透明度混淆；
- 原始职责和匹配任务重复计分；
- 定性要求误当硬门槛；
- 经验要求没有结构字段；
- 未体验偏好误标 confirmed；
- 兴趣误写成技能；
- 未证明误写成能力缺口；
- 未知条件误写成结构性冲突；
- 相邻任务偏好过度推广；
- 重复学历条件。

完整错误记录见 `eval_outputs/error_log_v1.json`。

## 七 自动规则和规范化

当前已有：

- `validate_structured_output.py`：结构、证据引用、任务引用和语义检查；
- `normalize_job_profile.py`：删除重复申请条件并保留清理日志；
- `normalize_profile.py`：规范未体验偏好的置信度并保留清理日志。

规范化只处理确定、可逆、可记录的机械问题；不能静默修改职业判断。

## 八 模型与运行限制

- 测试模型为 gpt-5.5；
- 默认 gpt-6-astra 因本机 Codex CLI 版本不兼容未运行；
- 测试期间网络连接不稳定，部分调用经过重试；
- 没有升级 CLI、安装软件或修改用户配置；
- 结果不代表其他模型必然具有同样表现。

## 九 当前阶段结论

AI 产品设计已经从“文档结构”进入“真实回归验证”。三个模块已经形成第一条可工作的纵向链路：

```text
用户材料 → 个人画像
原始 JD → 岗位画像
个人画像 + 岗位画像 → 证据对齐
```

当前尚未实现：

- 用户页面；
- 数据库存储；
- FastAPI 接口；
- 简历适配模块；
- 其余 7 条 JD 的批量回归。

## 十 下一阶段建议

下一阶段进入最小工程实现，但仍按学习顺序推进：

以下工作已经完成：

- 纯 Python 透明计分函数；
- 5 项核心单元测试；
- 飞书和腾讯示例评分输出。

下一步建议：

1. 定义数据库表；
2. 用 FastAPI 暴露最小接口；
3. 接入模型调用；
4. 最后连接前端。

第一段核心代码应由用户参与理解和实现；配置、校验脚本和普通 CRUD 可以由 AI 辅助。
