# 岗位画像 Prompt 首轮回归报告

测试日期：2026-09-21

## 一 测试目标

验证 AI 能否将原始 JD 转换成固定结构的岗位画像，并检查是否编造事实、遗漏明确条件、混淆必须与优先、过度推断工作方式或错误引用证据。

## 二 测试环境

- Codex CLI：0.136.0；
- 测试模型：gpt-5.5；
- 推理强度：none；
- 会话：临时、只读、忽略项目规则；
- 输出：严格 JSON Schema；
- 输入隔离：模型只接收单条 JD、Prompt 和 Schema，不读取人工期望夹具。

默认模型 gpt-6-astra 因本机 CLI 版本不兼容未产生结果。测试没有升级或修改用户环境，改用兼容模型 gpt-5.5。测试期间网络连接多次重试，但最终三条岗位均生成有效结果。

## 三 测试样本

| JD | 特点 | 用途 |
|---|---|---|
| 飞书 FDE | AI、客户调研、低代码、销售支持交叉 | 测试新型岗位和标题透明度 |
| 腾讯商业分析 | 信息不完整、多事业群、研究与推进并存 | 测试未知保留和面试信息分类 |
| 投后管理 | 群内转发、任务繁杂、时长条件明确 | 测试来源风险、任务聚类和经验要求 |

## 四 第一轮结果

飞书和腾讯输出均符合当时 Schema，但发现：

1. 测试输入没有传来源元数据，导致公司和来源被标为未知；
2. 腾讯“远程面试”被误当成工作地点；
3. 从岗位职责推导的能力被误标成 must；
4. 标题一致性和普通学生可理解程度被混为一个字段；
5. 缺失字段列表包含薪资、转正等 MVP 暂未建模信息，产生噪声。

## 五 第二轮修正与结果

修正后：

- 飞书、腾讯和投后管理三条输出均通过 Schema；
- 公司、来源和招聘状态正确；
- 腾讯工作地点保持未知，远程面试进入 interview_mode；
- 腾讯职责推导能力改为 inferred 与 uncertain；
- 明确条件、优先项和工作方式未知基本正确；
- 所有主要职责与能力均有 evidence_id。

## 六 第二轮暴露的 Schema 问题

### 原始职责与偏好任务不能共用一层

逐条职责适合事实展示，但直接计分会重复计算。因此保留 tasks，并新增 task_clusters 作为偏好判断和评分单元。

### 定性要求不等于硬门槛

“学业成绩优异”没有明确 GPA 或排名阈值，不能像毕业年份一样自动拦截。因此 other_hard_conditions 改为 other_application_conditions，并增加 gate_type。

### 经验要求需要独立结构

投后管理的“投行、事务所或股权投资机构经历优先”曾只保留在证据表中。新增 experience_requirements 避免遗漏。

## 七 第三轮局部验证

使用升级后的 Schema 再次运行投后管理：

- 成功输出 6 条职责；
- 成功归并为 4 个任务簇；
- 正确提取 4 类优先实习经验；
- 正确提取 6 个月和每周 4 天；
- 正确提示来源为非官网、公司未知；
- 结构验证通过。

仍存在一个 P2 问题：本科及以上在读同时出现在 degree_requirements 和 other_application_conditions。需要增加重复条件校验器。

## 八 错误统计

| 严重度 | 数量 | 当前状态 |
|---|---:|---|
| P0 | 0 | 无编造事实或危险简历内容 |
| P1 | 5 | 已通过 Prompt 或 Schema 修复 |
| P2 | 3 | 2 项已修复，1 项待增加后置校验 |

详细记录见 `eval_outputs/error_log_v1.json`。

## 九 当前结论

岗位画像结构已经能覆盖三类差异明显的 JD，且结构输出稳定。测试证明 Schema 不能一次凭空设计完成，必须由真实错误反推字段。

当前还不能宣称岗位解析模块完成，因为最终 Schema 尚未对飞书和腾讯完成第三轮全量回归，也尚未建立证据引用完整性校验器、测试个人画像提取和证据对齐。

## 十 下一步

以下工作已经完成：

- 轻量结构、证据引用和语义校验器；
- 最终 Schema 下的飞书和腾讯回归；
- 个人画像 Prompt 两轮测试；
- 飞书证据对齐 Prompt 两轮测试；
- 机械规范化与清理日志。

下一步进入纯 Python 透明计分函数和单元测试，之后才编写 FastAPI 接口。
