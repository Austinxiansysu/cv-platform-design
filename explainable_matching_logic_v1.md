# 可解释匹配逻辑与输出结构 v1.1

更新日期：2026-09-21

状态：产品定义修订版。修正了当前招聘资格阻断低年级学生职业探索的问题。权重为可校准假设，将使用首批 10 条 JD 测试后调整。

## 一 这一步解决什么问题

系统需要在不虚构、不隐藏不确定性、不迷信单一分数的前提下回答：

1. 用户现实上能否申请；
2. 用户是否可能喜欢岗位的真实任务；
3. 用户目前是否有足够证据胜任；
4. 岗位是否值得作为下一次职业探索；
5. 为什么推荐或不推荐；
6. 哪些结论仍需用户确认。

## 二 为什么现在要做

个人画像和岗位画像已经建立。如果现在不冻结匹配逻辑，后续 Prompt、接口、数据库和结果页面都会围绕模糊的“匹配度”反复修改。

第一版不追求预测录取概率，而是提高投递决策质量。

## 三 使用模式

系统在分析前先确认用户目的。同一条 JD 在不同模式下可以产生不同结论。

### 1 职业探索模式

适用于低年级学生或暂时不准备投递的用户，回答“这类工作是否值得作为未来方向”。

- 当前届别、发布时间、最早到岗和当期出勤要求不阻断岗位分析；
- 仍然展示这些条件，但标记为当前招聘批次限制；
- 重点输出任务偏好、能力证据、探索价值和未来准备方向；
- 默认将岗位加入职业样本库，而不是当前投递清单。

### 2 当前投递模式

适用于用户准备在当前周期申请，回答“这条具体 JD 现在是否值得投”。

- 届别、地点、到岗时间、每周天数和实习时长作为申请门槛；
- 明确冲突会阻止当前投递建议；
- 仍然继续计算职业方向匹配，避免把“现在不能投”误写成“不适合这类工作”。

### 3 未来规划模式

适用于用户已经发现高兴趣岗位但当前资格不符，回答“未来出现同类岗位时应准备什么”。

- 提取相对稳定的任务和能力要求；
- 将届别和招聘日期视为时间快照；
- 输出能力与证据准备清单；
- 提醒未来岗位内容可能变化，不能把 2026 年 JD 当作 2029 年招聘承诺。

当前项目使用首批 10 条 JD 时，默认采用职业探索模式。这些 JD 是岗位原型样本，不要求用户现在具备投递资格。

## 四 总体结构

岗位方向分析和当前资格判断并行执行：

```text
输入完整性检查
    ↓
选择分析模式
    ↓
┌──────────────┬────────────────┐
│ 岗位方向匹配 │ 当前申请资格   │
│ 想做程度     │ 届别与时间     │
│ 当前准备度   │ 地点与工作方式 │
│ 探索价值     │ 其他硬性要求   │
└──────────────┴────────────────┘
    ↓
置信度与未知信息
    ↓
职业方向结论 + 当前行动建议
```

职业方向分数不包含当前届别、招聘日期和到岗时间。现实冲突只影响当前行动建议。例如，岗位任务高度喜欢但毕业年份不符，系统应输出“方向匹配较高，当前批次不可申请”，而不是降低职业方向匹配度。

## 五 当前申请资格

### 1 检查字段

- 毕业年份或招聘届别；
- 学历与专业硬要求；
- 最早到岗时间；
- 每周到岗天数；
- 连续实习时长；
- 工作地点和远程方式；
- 是否需要出差；
- JD 明确写出的加班或高强度；
- 语言、证书或其他硬性条件。

### 2 判断状态

| 状态 | 定义 | 处理方式 |
|---|---|---|
| 通过 | 所有明确硬条件均满足 | 可以进入当前投递决策 |
| 待确认 | 关键条件缺失或用户信息缺失 | 岗位方向照常分析，申请前补充信息 |
| 当前批次冲突 | 届别、到岗日期等时间快照不符 | 不投当前批次，不影响职业方向判断 |
| 结构性冲突 | 地点、工作方式或长期要求与稳定偏好冲突 | 不建议当前申请，并在方向分析中提示风险 |
| 偏好冲突 | 不违反硬条件，但与用户工作方式偏好明显冲突 | 继续分析并突出风险 |

补充判定规则：用户仅在寒暑假可实习，而 JD 没有写最早开始或实习窗口时，应判为“待确认”，不能根据当前正值学期直接判冲突，也不能仅因天数和月数表面相容就判通过。当前本科但岗位要求硕士在读，若用户长期教育路径尚未确认，则只表示当前岗位批次不可申请，归入“当前批次冲突”，不把它重复标成永久结构性缺口。

若已经存在明确的当期硬冲突，例如岗位要求连续 6 个月而用户当前最多 3 个月，则无论是否还有开始时间等未知条件，状态都应是“当前批次冲突”；未知项继续保留，但不能把明确冲突降级为“待确认”。

### 3 当前用户约束

- 学期中默认不实习；
- 只考虑寒暑假；
- 假期每周可实习 4 到 5 天；
- 可连续实习 2 到 3 个月；
- 接受广州、深圳或远程；
- 倾向不出差；
- 尽可能避免加班和高强度；
- 预计毕业年份暂按 2029 年，待最终确认。

### 4 冲突优先级

硬冲突示例：

- 明确只招 2027 届，而用户预计 2029 届；
- 要求连续 6 个月，而用户只能 2 到 3 个月；
- 要求学期中每周 5 天到岗；
- 只允许北京长期线下。

偏好冲突示例：

- 岗位明确高强度；
- 岗位要求频繁客户现场或出差；
- 工作以材料、档案和流程处理为主。

## 六 想做程度

想做程度回答“如果资格允许，这些真实任务是否值得用户投入时间”。

### 1 内部权重

| 子维度 | 权重 | 原因 |
|---|---:|---|
| 核心任务偏好 | 55% | 岗位名称和行业标签不能代替日常任务 |
| 工作方式偏好 | 30% | 用户较重视工作强度、成果感和执行方式 |
| 行业与职能兴趣 | 15% | 行业兴趣重要，但应低于任务本身 |

### 2 单项评分

任务偏好使用五档：

| 原始值 | 标准分 |
|---|---:|
| 强烈不喜欢 | 0 |
| 不太喜欢 | 25 |
| 不确定或未体验 | 不计分，降低置信度 |
| 比较喜欢 | 75 |
| 非常喜欢 | 100 |

岗位任务按重要性加权：

- 核心任务权重 3；
- 重要支持任务权重 2；
- 一般支持任务权重 1。

### 3 防止公司光环

系统分别询问：

- 如果隐藏公司名称，你是否仍想做这些任务；
- 公司品牌吸引力；
- 任务吸引力。

公司品牌不进入想做程度，只作为附加背景展示。

## 七 当前准备度

当前准备度回答“用户能否用真实证据证明自己可以完成岗位任务”，不等于长期潜力。

### 1 内部权重

| 子维度 | 权重 | 原因 |
|---|---:|---|
| 核心任务的经历证据 | 50% | 真实做过相近任务最有说服力 |
| 技术和工具证据 | 30% | 本科生技能可以学习，不应压倒任务与经验 |
| 行业和业务知识 | 20% | 影响上手速度，但部分岗位可在实习中学习 |

### 2 证据强度

| 证据等级 | 标准分 | 定义 |
|---|---:|---|
| 直接强证据 | 100 | 真实场景中做过高度相似任务，有可验证成果 |
| 直接基础证据 | 75 | 做过相似任务，但复杂度或责任范围较低 |
| 可迁移证据 | 50 | 比赛、课程或其他项目证明了相关底层能力 |
| 自述无成果 | 25 | 用户表示会，但缺少项目或交付物 |
| 明确没有 | 0 | 用户确认没有相应能力或经历 |
| 信息未知 | 不计分 | 降低置信度，提示补充信息 |

### 3 语言要求

输出必须使用：

- 已有证据；
- 部分证据；
- 证据缺口；
- 明确能力缺口；
- 信息未知。

简历没有写 SQL 时，不能直接说“不会 SQL”；只能说“当前没有 SQL 证据”，除非用户明确确认不会。

## 八 职业探索价值

职业探索价值回答“即使当前不是最强候选人，这段实习是否值得用来验证重要职业假设”。

### 1 内部权重

| 子维度 | 权重 |
|---|---:|
| 验证优先职业假设 | 40% |
| 能力成长价值 | 60% |

### 2 判断标准

高探索价值：

- 能验证用户优先职业方向；
- 能接触尚未验证但重要的任务；
- 缺口可以在实习中合理学习；
- 工作成果和反馈比较清晰。

低探索价值：

- 主要重复用户已经明确不喜欢的任务；
- 与优先方向关系很弱；
- 名称有吸引力但职责信息严重不足；
- 所谓成长只来自招聘宣传，缺少职责证据。

## 九 职业方向分数

### 1 计算权重

只要岗位任务信息覆盖足够，无论当前招聘资格是否冲突，都可以计算：

```text
职业方向匹配摘要 = 想做程度 × 45%
                 + 当前准备度 × 30%
                 + 职业探索价值 × 25%
```

这一权重体现当前产品目标：本科生选择实习时，任务偏好和探索价值应高于当前技能数量。

### 2 显示规则

- 分数四舍五入到最接近的 5 分；
- 不显示 73、87 等虚假精确值；
- 同时显示三个独立分数；
- 当前申请资格单独展示；
- 信息覆盖不足时显示分数区间或暂不评分；
- 届别、发布时间、当期到岗日期不进入职业方向分数；
- 当前资格冲突不能把高方向匹配降成低匹配。
- 想做程度先计算“已知偏好覆盖率”：已明确喜欢或不喜欢的任务簇权重 ÷ 全部任务簇权重；
- 已知偏好覆盖率低于 50% 时，不显示想做程度数值，也不计算总体方向摘要；
- 此时显示“已知任务高兴趣或低兴趣，但整体偏好待验证”，并列出尚未体验的核心任务；
- unknown 和 untested 不得为了生成总分而自动填成中性分。

### 3 匹配等级

| 方向摘要 | 等级 | 含义 |
|---:|---|---|
| 80 到 100 | 高匹配 | 任务、证据和探索价值整体较好 |
| 65 到 75 | 中高匹配 | 值得优先了解，存在可解释缺口 |
| 50 到 60 | 条件匹配 | 需要明确关键缺口或偏好风险 |
| 35 到 45 | 低匹配 | 通常低优先级，但可能有单项探索价值 |
| 0 到 30 | 明显不匹配 | 任务或证据存在较大差距 |

分数处于档位边界时，不应因为 5 分差异改变建议，优先看证据和用户选择。

## 十 信息置信度

匹配结果同时输出置信度：高、中、低。

### 1 影响因素

- JD 是否完整；
- 来源是否可靠；
- 核心任务是否有原文证据；
- 用户画像是否经过确认；
- 用户能力是否有可验证成果；
- 是否存在内部矛盾；
- 未知字段是否影响决策。

### 2 降低置信度但不降分

以下情况降低置信度，不直接视为不匹配：

- JD 没写工作强度；
- 用户没有填写某项技能；
- 岗位没写任务占比；
- 页面未说明具体事业群或团队；
- 用户没有体验过某种工作方式。

## 十一 双结论决策矩阵

### 1 基础决策

| 方向匹配 | 当前资格 | 职业方向结论 | 当前行动建议 |
|---|---|---|---|
| 高 | 通过 | 优先方向 | 优先申请或值得申请 |
| 高 | 待确认 | 优先方向 | 先补充信息 |
| 高 | 当前批次冲突 | 优先方向 | 保存岗位原型，未来监控同类 JD |
| 高 | 准备度低 | 值得探索 | 建立能力与证据计划 |
| 低 | 通过 | 低优先方向 | 不因资格满足而强行申请 |
| 低 | 冲突 | 低优先方向 | 不申请，不进入未来重点方向 |

### 2 推荐标签

系统输出两组标签，不能混为一个结论。

职业方向标签：

- 优先方向；
- 值得探索；
- 需要更多体验；
- 低优先方向；
- 不符合当前偏好。

当前行动标签：

- 优先申请；
- 值得申请；
- 挑战申请；
- 先补充信息；
- 保存岗位原型；
- 监控未来同类岗位；
- 低优先级；
- 不建议申请；
- 当前批次不具备资格。

## 十二 缺口分类

### 1 当前批次缺口

明确不满足当前 JD 的届别、发布日期、开始时间或当期出勤要求。这类缺口不代表职业方向不匹配。

### 2 结构性可行性缺口

地点、出差、长期工作方式或稳定时间要求与用户长期偏好冲突。

### 3 能力缺口

用户明确不具备岗位必需技能。

### 4 证据缺口

用户可能具备能力，但没有项目、经历或成果证明。

### 5 偏好风险

用户可能不喜欢岗位的任务或工作方式。

### 6 信息缺口

JD 或用户画像没有足够信息。

输出必须使用正确缺口类型，不能把所有缺口都写成“能力不足”。

## 十三 当前投递推荐阈值

### 优先申请

- 现实可行性通过；
- 想做程度不低于 75；
- 当前准备度不低于 60；
- 没有未解决的关键风险。

### 挑战申请

- 现实可行性通过；
- 想做程度不低于 75；
- 当前准备度为 35 到 55；
- 主要缺口属于可迁移能力或证据不足，而非全部核心技能缺失。

### 先补充信息

- 现实可行性待确认；
- 或关键任务、强度、地点和职责范围未知；
- 或 JD 内部存在矛盾。

### 保存岗位原型或未来方向

- 想做程度和探索价值较高；
- 但存在当前届别、招聘日期或能力阶段冲突；
- 相似岗位在未来可能适合。

### 不建议申请

- 职业方向匹配和探索价值均低；
- 或核心任务偏好低；
- 或岗位主体是用户已明确不喜欢的工作。

## 十四 第一版输出结构

### 1 顶部结论

示例：

> 这类岗位与职业方向较匹配，但当前招聘批次不适用。你对 AI 方案搭建和真实业务问题兴趣较高，现有网站项目提供部分证据；当前 JD 的 2027 届限制只影响现在申请，不代表你不适合未来同类岗位。

### 2 当前申请资格

- 状态；
- 通过项；
- 冲突项；
- 待确认项；
- 对当前申请建议的影响；
- 是否属于只在本批次有效的时间条件。

### 3 三个独立维度

| 维度 | 分数或等级 | 置信度 | 一句话解释 |
|---|---|---|---|
| 想做程度 | 例如 85 | 中 | 喜欢核心方案任务，培训支持偏好未知 |
| 当前准备度 | 例如 55 | 中 | 有 AI 项目，但缺少企业客户和 SOP 证据 |
| 探索价值 | 例如 90 | 高 | 能验证 AI 业务解决方案方向 |

### 4 证据对照表

| 岗位要求或任务 | JD 证据 | 用户证据 | 判断 | 缺口类型 |
|---|---|---|---|---|
| AI 方案搭建 | JD 原文 | 企业官网项目 | 部分匹配 | 证据缺口 |
| 企业客户调研 | JD 原文 | 无直接证据 | 未证明 | 证据缺口 |
| 2027 届 | JD 原文 | 预计 2029 届 | 冲突 | 硬性缺口 |

### 5 高度匹配部分

最多三项，每项必须同时引用岗位证据和用户证据。

### 6 部分匹配部分

说明为什么不是完全匹配，以及可以如何补证据。

### 7 明显缺口

按硬性、能力、证据、偏好和信息缺口分类。

### 8 风险与未知

明确哪些结论不能从 JD 判断，以及申请前应该问什么。

### 9 双结论

- 职业方向标签；
- 当前行动标签；
- 方向推荐理由；
- 最重要的反对理由；
- 下一步行动。

## 十五 简历适配接口

匹配结果传给简历适配模块时，只能提供：

- 应突出哪些已有经历；
- 哪些任务与用户证据有关；
- 哪些关键词可以在真实经历中使用；
- 哪些缺口不能通过改写解决；
- 哪些事实禁止添加。

没有证据的岗位要求不能自动写入简历。

## 十六 防幻觉规则

1. 每个匹配结论必须引用 JD 和个人画像证据；
2. 不从公司名称推断工作强度；
3. 不把优先项当成硬性要求；
4. 不把简历没写当成用户不会；
5. 不把比赛经历夸大为企业工作经验；
6. 不把使用 AI 工具夸大为 AI 产品经理经验；
7. 不根据学校自动推断能力；
8. 不使用性别等敏感字段参与能力评分；
9. 不用其他维度抵消硬性资格冲突；
10. 每条推荐都提供至少一个反对理由。

## 十七 三条人工基准结果

以下分数是产品测试基准，不是客观职业结论。

### 1 飞书 FDE

| 项目 | 人工基准 |
|---|---|
| 当前申请资格 | 当前批次冲突，岗位要求 2027 届 |
| 想做程度 | 已知的方案搭建任务为高兴趣，但任务偏好覆盖不足 50%，暂不计分 |
| 当前准备度 | 55 |
| 探索价值 | 高 |
| 职业方向摘要 | 暂不计算数值 |
| 职业方向标签 | 值得探索，需验证客户调研、SOP 和培训任务 |
| 当前行动标签 | 当前批次不申请，保存岗位原型 |
| 关键理由 | 喜欢 AI 方案搭建，但客户调研、SOP 和培训任务偏好尚未验证 |

### 2 腾讯商业分析

| 项目 | 人工基准 |
|---|---|
| 当前申请资格 | 待确认，不影响方向分析 |
| 想做程度 | 80 |
| 当前准备度 | 55 |
| 探索价值 | 85 |
| 职业方向摘要 | 75 |
| 职业方向标签 | 优先方向或值得探索 |
| 当前行动标签 | 不作为当前投递样本，保留岗位原型 |
| 关键理由 | 商业与结构化分析兴趣较高，但事业群、届别、地点、数据工具和责任范围未知 |

### 3 投后管理

| 项目 | 人工基准 |
|---|---|
| 当前申请资格 | 当前时间冲突，要求 6 个月且每周 4 天 |
| 想做程度 | 40 |
| 当前准备度 | 25 |
| 探索价值 | 45 |
| 职业方向摘要 | 35 |
| 职业方向标签 | 低优先方向 |
| 当前行动标签 | 不建议申请 |
| 关键理由 | 材料、合规和协调占比较高，直接证据不足，时间条件也不符合 |

## 十八 验收测试

未来 AI 对三条基准 JD 的结果应满足：

### 飞书 FDE

- 识别为 AI 解决方案和销售支持交叉岗位；
- 不直接称为 AI 产品经理；
- 发现招聘届别冲突但不降低方向分；
- 给出保存岗位原型和未来监控，而非简单不匹配。

### 腾讯商业分析

- 不因为腾讯品牌自动提高任务匹配；
- 识别行业、用户、战略和项目落地任务；
- 对事业群、地点、届别和数据工具保持未知；
- 对当前申请资格保持待确认，但仍完成方向分析。

### 投后管理

- 识别材料、募资、合规和 PR、GR 支持；
- 不把岗位概括成投资研究；
- 识别 6 个月和每周 4 天的时间冲突；
- 输出不建议申请。

## 十九 下一步

完成这一逻辑后，产品定义阶段的核心业务规则已经闭环。下一阶段进入：

1. 设计 AI 结构化输出格式；
2. 设计个人画像 Prompt；
3. 设计岗位解析 Prompt；
4. 使用 10 条 JD 做人工与 AI 对照测试；
5. 通过错误案例修改字段和规则；
6. 之后才开始后端和数据库实现。
