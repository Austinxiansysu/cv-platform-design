# DeepSeek 岗位解析真实调用回归 v1

日期：2026-09-24

## 测试范围

使用用户在本机页面输入的 DeepSeek API Key，经本机 `POST /jobs/analyze` 调用 `deepseek-flash`。只提交了三条岗位 JD 文本：JD-007 飞书 FDE、JD-009 腾讯商业分析、JD-002 投后管理。没有提交个人画像或简历；没有自动保存失败草稿。

## 第一轮结果

| 样本 | 调用结果 | 发现 |
|---|---|---|
| JD-007 飞书 FDE | HTTP 502，本地语义校验拒绝 | JSON 结构可用；两项从职责推断的能力被写成过强要求，一项学历条件重复 |
| JD-009 腾讯商业分析 | HTTP 502，非合法 JSON | 需识别模型返回的具体形态并决定是否可安全重试 |
| JD-002 投后管理 | HTTP 502，模型请求失败 | 并发测试和单独重试均失败；需读取服务商 HTTP 状态和安全裁剪后的错误信息 |

本轮未能获得一条可保存的完整草稿，因此不能宣称真实 DeepSeek 解析链路已通过。

## 已作修复

新增后置机械归一化，只有在字段含义明确时才调整：推断能力的要求强度改为不确定；移除与学历字段重复的条件；“至少 X–Y 天”不生成到岗上限；单一实习时长不重复作为偏好值。每项修改写入岗位画像 `job_uncertainties.warnings`，便于用户核对。

岗位解析接口还会区分服务商 HTTP 错误、连接失败、超时和非 JSON 输出。非 JSON 错误仅显示脱敏且裁剪的片段；不会打印 API Key。结构错误和其余语义错误仍会拒绝入库。

根据 DeepSeek 官方说明，`deepseek-flash` 的思考模式默认是 high。JD 结构化提取改用 `reasoning.effort=low`，并明确要求不附加 Markdown 或说明，以减少长推理占用输出预算；这一调整还没有经过真实调用验证。

## 下一道门槛

后端重启会清除内存中的 Key。用户重新在本机页面输入后，先顺序重测 JD-007 与 JD-002：

1. JD-007 应通过本地结构与语义校验，再与人工基准核对职能、核心任务、届别与未知项；
2. JD-002 先确认服务商 HTTP 状态及错误原因；
3. 仅在两条稳定后扩大到其余 JD，控制不必要的重复调用。

测试输出放在被 Git 忽略的 `eval_outputs/deepseek-live-001/`；报告不包含 Key 或个人简历。
